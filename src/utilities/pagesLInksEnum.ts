export enum LinksEnum {
  home = "/",
  login = "/login",
  logout = "/logout",
  recover = "/recover",

  dashboard = "/dashboard",
  products = "/products",
  orders = "/orders",
  entries = "/entries",
  exits = "/exits",
  deliveries = "/deliveries",
  couriers = "/couriers",
  distributors = "/distributors",
  stockists = "/stockists",
  notifications = "/notifications",
  profile = "/profile",
}
